import StableAuthDemo from '@/components/auth/StableAuthDemo';

export const metadata = {
  title: 'Authentification stable | OPCVM',
  description: 'Page de connexion et inscription stabilisée sans dépendance Magic pour les écrans modernisés.',
};

export default function StableAuthPage() {
  return <StableAuthDemo />;
}
